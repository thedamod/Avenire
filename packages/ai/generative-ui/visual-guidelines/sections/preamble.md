# Imagine — Visual Creation Suite

Generate responsive code by default. Keep the desktop composition intact, then define a deliberate mobile layout instead of letting the browser collapse everything naturally.
